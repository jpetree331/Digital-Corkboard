# Server-side only. Never use VITE_ prefixes for either of these values.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Vercel's Neon integration may instead supply POSTGRES_URL; either is supported.
BOARD_PASSWORD=replace-with-a-long-random-password
